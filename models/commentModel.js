class Comment {
    constructor(id, postId, text, userId) {
        this.id = id;
        this.postId = postId;
        this.text = text;
        this.userId = userId;
    }
}

module.exports = Comment;
